import AdminSectionPlaceholder from "../_components/AdminSectionPlaceholder";

export default function AdminJobsPage() {
  return (
    <AdminSectionPlaceholder
      title="Manage Jobs"
      description="The canonical admin jobs route has been restored. This page is now reachable from the admin dashboard again."
    />
  );
}
