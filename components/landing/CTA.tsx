import styles from './landing.module.css';

const CTA = () => {
    return (
        <section className={styles.cta}>
            {/* CTA content goes here */}
        </section>
    );
};

export default CTA;
